const currentMeetings: number[][] = [[1000, 1200], [1415, 1530], [1600, 1630]];

const findFreeMeetingSlot = (meetings: number[][], meetingLength: number) => {};

console.log(findFreeMeetingSlot(currentMeetings, 45));
